@Library('vietlink-jenkins-pipeline-library-dev') _

pipeline {
    agent any

    stages {
        stage('Build'){
            steps {
               sh 'echo pass'
            }
        }
        stage('Deploy'){
            steps{
                sh 'echo pass'
            }
        }
    }
    post {
        always {
            echo "Fail"
        }
    }
}
