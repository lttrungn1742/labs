pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'docker -v'
            }
        }
    }
}

// node {
//     stage("Test"){
//         sh 'docker -v'
//     }

// }